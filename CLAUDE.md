# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트에서 작업할 때 따라야 하는 메타 규칙을 정의합니다.

---

## 🔀 두 가지 유즈케이스

이 레포지토리는 **두 가지 완전히 다른 목적**으로 사용됩니다:

| 유즈케이스 | 목적 | 사용 방법 |
|-----------|------|----------|
| **1. 연구자 모드** | 패스파인더를 사용해 주제를 심층 조사 | `/dr [질문]` 스킬 사용 |
| **2. 개발자 모드** | 패스파인더 자체를 개발/수정 | 스펙 문서 기반 구현 |

### 유즈케이스 판단 방법

```
사용자 요청 분석:
├─ "~에 대해 조사해줘", "/dr", "리서치해줘" → 연구자 모드
└─ "기능 추가해줘", "버그 수정", "스킬 만들어줘" → 개발자 모드
```

---

# 유즈케이스 1: 연구자 모드 (Pathfinder 사용)

패스파인더를 도구로 사용하여 심층 연구를 수행하는 모드입니다.

## 사용 가능한 스킬

| 스킬 | 설명 |
|------|------|
| `/dr [질문]` 또는 `/deep-research [질문]` | 심층 연구 시작 (무한 반복) |
| `/research-status` | 현재 연구 진행 상태 확인 |
| `/research-report` | 연구 결과 보고서 생성 |
| `/research-resume` | 중단된 연구 재개 |

## 연구 흐름 (자동화)

```
/dr "연구 주제" 입력
→ 연구 사이클 자동 시작 (무한 반복)
  ├─ LOAD: state.json 로드
  ├─ REFLECT: Extended Thinking으로 분석
  ├─ PLAN: 검색 쿼리 생성
  │   └─ 🔄 deduplicate_search.py 자동 호출 (중복 제거)
  ├─ EXECUTE: 병렬 검색 실행
  │   └─ 🔄 add_query_to_history() 자동 저장
  ├─ VERIFY: Cross-validation, 신뢰도 체크
  ├─ SYNTHESIZE: 지식 통합
  │   └─ 🔄 knowledge_tracker.py 자동 호출 (모순 탐지)
  ├─ SAVE: 모든 상태 저장
  ├─ OUTPUT: 진행 상황 출력
  └─ LOOP: 다음 iteration (자동)
      └─ 🔄 stop-hook.py가 종료 조건 체크

→ 사용자가 중단하거나 completion criteria 만족 시 종료
→ /research-report로 결과 확인
```

## 자동 실행 기능 (물처럼 흐르는 구조)

사용자가 `/dr`만 입력하면 다음 기능들이 **자동으로** 필요한 시점에 실행됩니다:

| 기능 | 실행 시점 | 파일 | 역할 |
|------|----------|------|------|
| **중복 검색 제거** | PLAN 단계 | `deduplicate_search.py` | 이미 검색한 쿼리 필터링 (>0.95 유사도) |
| **검색 히스토리** | EXECUTE 단계 | `deduplicate_search.py` | 검색 후 embedding과 함께 자동 저장 |
| **모순 탐지** | SYNTHESIZE 단계 | `knowledge_tracker.py` | 발견 사항 간 모순 자동 탐지 (>0.85 유사도) |
| **지식 그래프** | SYNTHESIZE 단계 | `knowledge_tracker.py` | 발견 사항을 노드로 저장, 관계 매핑 |
| **종료 조건 체크** | 매 턴 종료 시 | `stop-hook.py` | Objective criteria 만족 여부 판단 |
| **자동 반복** | OUTPUT 후 | `SKILL.md` | 종료 조건 불만족 시 다음 iteration 자동 시작 |

**핵심:** 사용자는 `/dr [질문]` 한 번만 입력하면, 나머지는 모두 자동으로 실행됩니다.

## Progressive Disclosure (정보 접근)

연구 결과는 2-Level 구조로 조직화됩니다:

**Level 1 (Essential - 즉시 확인):**
- `.research/INDEX.md` - 핵심 발견 10개, 가설 요약, 진행도 (50 lines)
- `/research-status` 스킬 - 현재 상태 즉시 출력

**Level 2 (Optional - 상세 정보):**
- `.research/topics/*.md` - 주제별 상세 설명, 코드, 출처
- `.research/knowledge_graph.json` - 전체 지식 그래프
- `.research/iteration_logs/` - 반복별 상세 로그

**사용 예:**
```
# 빠른 확인 (Level 1)
/research-status

# 특정 주제 상세 (Level 2)
cat .research/topics/ralph_loop.md

# CLI로 모순 확인 (Level 2)
python .research/knowledge_tracker.py contradictions
```

## 연구자 모드에서 주의사항

- 연구 중에는 `.research/` 디렉토리에 상태가 저장됨
- 연구는 사용자가 명시적으로 중단하거나 completion criteria 만족 시까지 계속됨
- 중간에 `/research-status`로 진행 상황 확인 가능
- `.research/` 파일 수동 수정 금지 (상태 손상 가능)

## 사고도구 (Thinking Tools) 사용법

연구 중 막히거나 방향 전환이 필요할 때 자동으로 활용되는 사고 프레임워크:

| 상황 | 도구 | 설명 | 예시 |
|------|------|------|------|
| 막힐 때 | **제1원칙** | 근본 원리부터 재구성 | "LLM 창의성 측정 → Novelty + Usefulness로 분해" |
| 정보 과다 | **오컴의 면도날** | 단순한 설명 우선 | "API 지연 → 복잡한 인프라보다 N+1 쿼리 먼저 확인" |
| 확신 생길 때 | **반증 가능성** | 반박 증거 탐색 | "Transformer 최고 → 실패 사례 적극 검색" |
| 새 방향 필요 | **과학적 방법론** | 관찰→가설→실험 | "성능 저하 → 벤치마크 → 원인 분석 → 최적화" |
| 증거 업데이트 | **베이지안 추론** | 확신도 업데이트 | "신뢰도 높은 소스 반박 → 확신도 -20%" |
| 상충 관점 | **변증법적 사고** | 정-반-합 통합 | "원격 vs 사무실 → 하이브리드 모델" |

**상세 가이드 및 18개 실전 예제:**
`.claude/skills/deep-research/references/thinking_tools.md`

**자동 적용:**
- REFLECT 단계에서 상황 분석 시 자동 선택
- 각 도구별 3개 실전 예제로 즉시 활용 가능
- 확신도 >80% 시 반증 가능성 자동 활성화

---

# 유즈케이스 2: 개발자 모드 (Pathfinder 개발)

패스파인더의 기능, 스킬, 훅 등을 개발/수정하는 모드입니다.

## 🎯 핵심 원칙

**모든 스펙과 구현 세부사항은 `docs/spec/` 디렉토리에 있습니다.**
CLAUDE.md는 "스펙 문서를 어떻게 사용할지"만 정의합니다.

**진입점:** `docs/spec/index.md` - 프로그레시브 디스클로저 방식의 스펙 네비게이션 허브

---

## 📋 작업 프로세스 (절대 규칙)

### 1. 요청 받을 때
```
사용자 요청 → docs/spec/index.md 먼저 읽기 → 관련 스펙 문서 탐색 → 스펙 기반 판단/구현
```

**필수:**
- 어떤 기능 요청이든 `docs/spec/index.md`를 먼저 확인하여 관련 문서 위치 파악
- 프로그레시브 디스클로저 방식: 필요한 정보만 선택적으로 읽기
  - 예: Ralph Loop 수정 → index.md의 "빠른 찾기" → 03-ralph-loop.md 읽기
  - 예: 검증 로직 수정 → index.md에서 05-verification.md 찾기
- 스펙에 정의된 아키텍처, 데이터 구조, 동작 방식 준수
- 스펙에 없는 내용이면 사용자에게 스펙 추가 여부 확인

### 2. 구현 완료 후
```
코드 작성 완료 → 관련 스펙 문서 업데이트 (필수) → 커밋
```

**필수:**
- 구현 내용을 해당 스펙 문서에 반영 (파일 경로, 함수명, 데이터 스키마 등)
  - Stop Hook 수정 → `docs/spec/03-ralph-loop.md` + `docs/spec/12-hooks-api.md` 업데이트
  - 연구 사이클 수정 → `docs/spec/04-research-cycle.md` 업데이트
  - state.json 스키마 변경 → `docs/spec/07-data-schemas.md` 업데이트
- 변경된 동작이 있으면 스펙 문서에도 업데이트
- **spec 업데이트 없이 구현만 하는 것은 금지**
- 업데이트 시 문서 상단의 "최종 수정일" 갱신

### 3. 검증 단계
```
주기적으로: 실제 코드 ↔ 해당 스펙 문서 일치 여부 확인
```

**확인 항목:**
- 파일 구조가 스펙과 동일한지 (`docs/spec/02-architecture.md` 참조)
- 함수/클래스 시그니처가 스펙과 일치하는지
- 데이터 스키마(JSON 구조 등)가 스펙과 동일한지 (`docs/spec/07-data-schemas.md` 참조)
- 불일치 발견 시 즉시 보고 및 수정

---

## 📖 프로그레시브 디스클로저 활용법

### 정보 탐색 전략

**1단계: 시작점 (index.md)**
```
docs/spec/index.md 읽기
→ "빠른 시작" 테이블에서 역할 확인
→ "주제별 빠른 찾기"에서 키워드 검색
```

**2단계: 관련 문서로 이동**
```
예: "Ralph Loop 수정 필요"
→ index.md > "Ralph Loop 관련" > "무한 루프 구현"
→ 03-ralph-loop.md > "Stop Hook 구현" 섹션 읽기
```

**3단계: 깊이 조절**
```
- 개요만 필요 → 해당 문서의 "개요" 섹션만 읽기
- 상세 구현 → "구현 메커니즘" 섹션까지 읽기
- 전체 이해 → 문서 전체 + 관련 문서 링크 따라가기
```

### 일반적인 작업별 문서 경로

| 작업 | 문서 경로 |
|------|----------|
| 새 기능 추가 | index.md → 02-architecture.md → 해당 컴포넌트 문서 |
| 버그 수정 | index.md → 관련 기능 문서 → 구현 섹션 |
| 성능 최적화 | 02-architecture.md → 04-research-cycle.md |
| 설정 변경 | 08-configuration.md |
| 데이터 구조 확인 | 07-data-schemas.md |
| 테스트 작성 | 09-testing.md |

### 스펙 업데이트 규칙

코드 변경 시 반드시 다음 문서 업데이트:

| 변경 영역 | 업데이트할 문서 |
|----------|----------------|
| Stop Hook | 03-ralph-loop.md, 12-hooks-api.md |
| 연구 사이클 | 04-research-cycle.md |
| 검증 로직 | 05-verification.md |
| state.json | 07-data-schemas.md |
| config.json | 08-configuration.md |
| 스킬 추가/수정 | 11-skills-api.md |

---

## 🚫 금지 사항

1. **스펙 무시 구현**: 스펙 문서를 읽지 않고 임의로 구현
2. **스펙 미업데이트**: 코드만 수정하고 관련 스펙 문서 업데이트 안 함
3. **스펙과 불일치 방치**: 실제 구현과 스펙이 다른 상태 유지
4. **잘못된 문서 참조**: `docs/spec.md` 대신 `docs/spec/index.md` 사용 필수

---

## 📝 작업 예시

### ✅ 올바른 흐름
```
사용자: "연구 상태 저장 기능 추가해줘"
→ Read docs/spec/index.md (빠른 찾기에서 "데이터 구조" 섹션 확인)
→ Read docs/spec/07-data-schemas.md (state.json 스키마 확인)
→ 스키마 준수하여 구현
→ docs/spec/07-data-schemas.md 업데이트 (새 필드 추가 반영)
→ 문서 상단 "최종 수정일" 갱신
→ 커밋
```

### ❌ 잘못된 흐름
```
사용자: "연구 상태 저장 기능 추가해줘"
→ 바로 코드 작성 (spec 확인 안 함)
→ 임의로 JSON 구조 설계
→ 스펙 문서 업데이트 없이 커밋 ← 금지!
```

---

## 🔧 프로젝트 정보

- **프로젝트명**: Pathfinder Deep Research
- **버전**: v5 (Memory Blocks Enhanced)
- **스펙 문서**: `docs/spec/` 디렉토리 (프로그레시브 디스클로저)
  - **진입점**: `docs/spec/index.md`
  - **주요 문서**: 01-overview.md, 02-architecture.md, 03-ralph-loop.md 등
  - **신규 (v5)**: 13-native-function-calling.md, Memory Blocks Architecture
- **언어**: Python, Bash, Markdown
- **주요 디렉토리**:
  - `.research/`: 연구 상태 저장 (실행 중 생성)
    - `working_memory.json`: 최근 10 iterations (HOT)
    - `findings.md`: 핵심 발견 (Semantic Memory)
    - `archival/`: 전체 iteration 로그 (COLD)
    - `memory_manager.py`: 3-tier Memory 관리
    - `deduplicate_search.py`: 검색 중복 제거
    - `knowledge_tracker.py`: 지식 그래프 및 모순 탐지
  - `.claude/skills/`: Claude Code 스킬 정의
  - `.claude/hooks/`: Stop Hook (Ralph Loop 구현)
  - `docs/spec/`: 스펙 문서 (컴포넌트별 분리)

---

## 💡 요약

### 연구자 모드
- `/dr [질문]`으로 심층 연구 시작
- `/research-status`, `/research-report` 등으로 진행 관리
- 스펙 문서 수정 불필요

### 개발자 모드
1. **모든 판단의 기준 = docs/spec/ 디렉토리 (진입점: index.md)**
2. **프로그레시브 디스클로저**: 필요한 정보만 선택적으로 읽기
3. **구현 후 반드시 해당 스펙 문서 업데이트**
4. **spec과 코드의 일치성 유지 필수**

## 📚 스펙 문서 활용 가이드

### 빠른 시작

1. **처음 사용**: `docs/spec/index.md` → "빠른 시작" 섹션
2. **기능 추가/수정**: `index.md` → "주제별 빠른 찾기" → 관련 문서
3. **아키텍처 이해**: `02-architecture.md` 읽기
4. **특정 기능 찾기**: `index.md`의 "문서 목록" 또는 Ctrl+F 검색

### 자주 찾는 스펙

- **Ralph Loop (무한 루프)**: `03-ralph-loop.md`
- **연구 사이클 (9단계)**: `04-research-cycle.md`
- **검증 시스템**: `05-verification.md`
- **데이터 스키마**: `07-data-schemas.md`
- **설정**: `08-configuration.md`
- **🆕 Memory Blocks (v5)**: `07-data-schemas.md` > "Memory Blocks Architecture"
- **🆕 Native Function Calling**: `13-native-function-calling.md`

### v5 신규 기능 (RESEARCH_REPORT 권장사항 적용)

1. **Observation Masking (10 turns)**
   - 위치: `.research/memory_manager.py`, `SKILL.md`
   - 효과: 컨텍스트 67% 감소, 비용 절감

2. **Memory Blocks 구조 (3-tier)**
   - Working Memory: `working_memory.json` (최근 10 iterations)
   - Semantic Memory: `findings.md` (핵심 발견)
   - Archival Memory: `archival/*.json` (전체 로그)

3. **Native Function Calling 표준**
   - ReAct 패턴 대체 (30% → 95% 성공률)
   - 병렬 도구 호출 지원

**상세:** `IMPLEMENTATION_SUMMARY.md` 참조
