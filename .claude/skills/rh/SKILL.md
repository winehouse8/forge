---
name: rh
description: rabbit-hole의 짧은 버전. 토끼굴 탐험가.
argument-hint: [research question]
allowed-tools: WebSearch, WebFetch, Read, Write, Edit, Bash, Glob, Grep, Skill
---

# 🐰 rh (Rabbit Hole shorthand)

`rabbit-hole` 스킬의 짧은 버전입니다.

사용법:
```
/rh "양자 컴퓨팅 실용화 시기?"
```

**자세한 내용:**
`/rabbit-hole` 또는 `.claude/skills/rabbit-hole/SKILL.md` 참조

---

**즉시 rabbit-hole 스킬 호출:**

```
Skill(skill="rabbit-hole", args="$ARGUMENTS")
```
