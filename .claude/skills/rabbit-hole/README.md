# 🐰 Rabbit-Hole

**Deep Research Agent** - 질문을 던지면 자동으로 심층 연구를 수행하는 AI 에이전트

## 빠른 시작

```bash
# 새 연구 시작
./rabbit-hole.sh "LLM 에이전트의 자율적 연구 능력과 한계"

# 이어서 하기
./rabbit-hole.sh --resume

# 최대 100 iteration
./rabbit-hole.sh "질문" 100
```

## 작동 방식

```
┌────────────────────────────────────────────┐
│  rabbit-hole.sh (외부 루프)                │
│       ↓                                    │
│  Claude 호출 (1 iteration)                 │
│       ↓                                    │
│  SPAWN → SELECT → EXPLORE → SAVE           │
│       ↓                                    │
│  iteration++ → 다음 루프                   │
└────────────────────────────────────────────┘
```

**한 번 호출 = 한 iteration**
- 외부 스크립트가 for 루프로 Claude 반복 호출
- 매번 깨끗한 컨텍스트로 시작 (비용 효율적)
- 모든 상태는 `.research/current/`에 파일로 저장

## 연구 사이클

### 1. SPAWN
pending holes가 3개 미만이면 6개 생성:
- **초기** (claim 없음): explore 6개
- **진행 중**: coverage 2 + verify 2 + trace 2

### 2. SELECT
interest가 높은 hole 선택

### 3. EXPLORE
hole 타입에 따라 다른 컨텍스트 로드:

| 타입 | 로드 | 검색 방향 |
|------|------|----------|
| explore | summary.md | 넓게 탐색 |
| verify | summary.md + target claim | 반증/한계 |
| trace | summary.md + target claim | 원문/스펙 |

### 4. SAVE
1. Evidence 저장
2. Claim 생성/갱신
3. Summary 갱신
4. holes.json 갱신
5. 상태 출력

## 판정 라벨

| 라벨 | 의미 | 행동 |
|------|------|------|
| **NEW** | 새 정보 | claim 생성 |
| **SUPPORTS** | 기존 claim 지지 | strength +0.1~0.3 |
| **REBUTS** | 기존 claim 반박 | strength -0.3 |
| **QUALIFIES** | 조건 추가 | claim에 "단, ~" 추가 |
| **SKIP** | 중복/저품질 | 무시 |

## 파일 구조

```
.research/
├── current -> sessions/research_YYYYMMDD_HHMMSS/
└── sessions/
    └── research_YYYYMMDD_HHMMSS/
        ├── summary.md          # 전체 지식 맵 (50줄)
        ├── holes.json          # 상태 관리
        ├── claims/
        │   └── claim_{N}.md    # 개별 주장
        └── evidence/
            └── ev_{N}.md       # 근거 자료
```

## 명령어

| 명령어 | 설명 |
|--------|------|
| `./rabbit-hole.sh "질문"` | 새 연구 시작 |
| `./rabbit-hole.sh --resume` | 이어서 하기 |
| `./rabbit-hole.sh -r` | --resume 단축 |
| `./rabbit-hole.sh "질문" 100` | max 100 iterations |
| `./rabbit-hole.sh --max 100` | max iterations 지정 |
| `Ctrl+C` | 중단 (언제든 재개 가능) |

## 완료 조건

- iteration ≥ 10 **AND** pending = 0
- 또는 max iterations 도달

## 출력 예시

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  🐰 Rabbit-Hole - Deep Research Agent                       ┃
┃     Iteration 5 of 50                                        ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

[INFO] Running iteration 5...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐰 Iteration 5
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🕳️ 탐색: LLM 에이전트의 알려진 한계점
📥 발견: [NEW] hallucination 문제, [QUALIFIES] 특정 도메인 한정
📋 현재: claims 8개, evidence 12개
⏳ pending: 4개
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Iteration 5 Summary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ Iteration: 5
  ✓ Claims: 8 | Evidence: 12
  ✓ Explored: 5 | Pending: 4
  🕳️ Last explored: LLM 에이전트의 알려진 한계점...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[INFO] Continuing in 2 seconds...
```

## 결과 확인

```bash
# 요약 보기
cat .research/current/summary.md

# 특정 claim 보기
cat .research/current/claims/claim_1.md

# 모든 claims 목록
ls .research/current/claims/

# holes 상태
jq . .research/current/holes.json
```

## vs Stop Hook 방식

| 측면 | 외부 스크립트 (현재) | Stop Hook |
|------|---------------------|-----------|
| 안정성 | ✅ 매우 안정적 | ❌ 버그 가능성 |
| 비용 | ✅ 효율적 | ❌ 컨텍스트 누적 |
| 제어 | ✅ 완전한 제어 | △ Claude 의존 |
| 중단/재개 | ✅ 쉬움 | △ 복잡 |

## 문제 해결

### 세션이 없다고 나올 때
```bash
# 새 세션으로 시작
./rabbit-hole.sh "새 질문"
```

### 기존 세션 확인
```bash
ls .research/sessions/
cat .research/current/holes.json
```

### 세션 정리
```bash
rm -rf .research/sessions/research_XXXXXX
# current symlink도 깨지면
rm .research/current
```

## 라이선스

MIT
